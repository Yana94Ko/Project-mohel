<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finalproject.mohel.dao.MemberDAO">
	<select id="searchNickname" resultType="int">
		select count(no) from member where nickname=#{param1}
	</select>
	<select id="searchEmail" resultType="int">
		select count(no) from member where email=#{param1}
	</select>
	<insert id="insertMember">
		insert into member(email, pwd, nickname, tel, profile, birthdate, gender, age, height, weight, BMR, AMR)
		values(#{email}, #{pwd}, #{nickname}, #{tel}, #{profile}, #{birthdate}, #{gender}, #{age}, #{height}, #{weight}, #{BMR}, #{AMR})
	</insert>
	<select id="selectMember" resultType="MemberVO">
		select no, email, nickname, tel, profile, birthdate, gender, age, height, weight, bmr, amr, verify
		from member where email=#{email}
		<if test="pwd!=null">
			 and pwd=#{pwd}
		</if>
	</select>
</mapper>